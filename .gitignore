node_modules
dist
dist-electron
release
projects
builds
vendor
.vscode
*.log
.DS_Store

# OAuth client credentials — never commit these.
google-oauth.json
client_secret_*.json
.env

# SMTP credentials for build-complete emails. The committed
# email-config.example.json is the template; the real file holds the
# Titan/BigRock mailbox password and must never be committed or shipped
# in a public installer (see comment in the example file).
email-config.json

# Claude Code MCP config embeds the Stripe secret key. Never commit.
.mcp.json

# Local Claude Code workspace state (per-machine, not project source).
.claude/

# electron-builder outputs (regenerated locally; too big for git).
build/
buildReleases/

# Vendored asset downloads (zips + extracted folders).
*.zip
banani-ui-export/
icons8-studentvue-ios-17-filled/

# Installers / binaries.
*.exe

# Stripe 2FA backup recovery code — sensitive, do not commit.
stripe_backup_code*.txt
